const renderLoader = () => {
  const loader = `<div className="list--item">
                    <h3 style="color:white;">
                    Loading...
                    </h3>
                    </div>`;

  return loader;
};

export default renderLoader;
